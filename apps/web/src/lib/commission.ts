// Flat platform commission rate applied to vendor sales on order completion.
// A per-category override table (as in the audited Botble marketplace plugin) is a later enhancement.
export const PLATFORM_COMMISSION_RATE = 0.1;

export const MIN_WITHDRAWAL_AMOUNT = 200;
